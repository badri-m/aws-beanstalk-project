# 1. Use an official NGINX image as the base
FROM nginx:alpine

# 2. Copy your HTML file into the NGINX web root directory
COPY index.html /usr/share/nginx/html

# 3. Expose port 80 (the default port for HTTP)
EXPOSE 80